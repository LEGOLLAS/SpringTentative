<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.gurwl.test.database.repository.StoreRepository">

	<select id="selectStoreList" resultType="com.gurwl.test.database.domain.Store">
		SELECT `id`
			, `name`
			, `phoneNumber`
			, `address`
			, `businessNumber`
			, `introduce`
			, `created_at`
			, `updated_at` 
		from 
			`store`	
	</select>

</mapper>